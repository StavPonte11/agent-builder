{
  "name": "agent-builder",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "codegen": "turbo run codegen",
    "clean": "turbo run clean && rm -rf node_modules",
    "test:vibe": "cd apps/agent-builder-backend && python tests/test_vibe.py",
    "test:seed": "cd apps/agent-builder-backend && python tests/seed_demo.py",
    "test:seed:fraud": "cd apps/agent-builder-backend && python tests/seed_demo.py --procurement"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "packageManager": "npm@10.9.2"
}
